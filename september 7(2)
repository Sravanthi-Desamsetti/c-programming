#include <stdio.h>
#include <stdlib.h>
int main()
{
    int i,n,sum=0,*arr;
    printf("Enter the number of elements : ");
    scanf("%d", &n);
    for (i=0;i<n;i++)
    {
        scanf("%d", arr+i);
        sum =sum+ *(arr+i);
    }
    printf("sum is %d \n", sum);

    return 0;
}

Enter the number of elements : 3
5
4
6
sum is 15
